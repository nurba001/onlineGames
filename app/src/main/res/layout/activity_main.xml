<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#121212"
    tools:context=".view.MainActivity">

    <!-- 1. Поиск -->
    <androidx.appcompat.widget.SearchView
        android:id="@+id/search_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:background="@drawable/bg_search_rounded"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:iconifiedByDefault="false"
        app:queryHint="Поиск по названию..."
        app:defaultQueryHint="Поиск по названию..." />

    <!-- 2. Блок Фильтров -->
    <LinearLayout
        android:id="@+id/filter_layout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginHorizontal="12dp"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/search_view"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <Spinner
            android:id="@+id/spinner_platform"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginEnd="4dp"
            android:background="@drawable/bg_search_rounded"
            android:popupBackground="@drawable/bg_search_rounded"
            android:prompt="@string/prompt_platform"
            android:entries="@array/platforms_array"/>

        <Spinner
            android:id="@+id/spinner_genre"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="4dp"
            android:background="@drawable/bg_search_rounded"
            android:popupBackground="@drawable/bg_search_rounded"
            android:prompt="@string/prompt_genre"
            android:entries="@array/genres_array"/>
    </LinearLayout>

    <!-- 3. Список Игр -->

    <!-- 4. Кнопка Избранное (ВНИЗУ СПРАВА) -->

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_games"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="1.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/filter_layout"
        app:layout_constraintVertical_bias="0.0"
        tools:listitem="@layout/item_game_card" />

    <ImageButton
        android:id="@+id/button_show_favorites_icon"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_margin="16dp"
        android:background="@drawable/bg_rating_badge"
        android:elevation="6dp"
        android:contentDescription="Избранное"
        android:src="@drawable/icon"
        android:scaleType="centerInside"
        android:padding="12dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />
    <!-- НОВОЕ: Плавающая кнопка ПЛЮС -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add_game"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="24dp"
        android:layout_marginEnd="16dp"
        android:contentDescription="Добавить игру"
        android:padding="12dp"
        android:scaleType="centerInside"
        android:src="@drawable/icon_add"
        app:backgroundTint="#4CAF50"
        app:layout_constraintBottom_toTopOf="@+id/button_show_favorites_icon"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/filter_layout"
        app:layout_constraintVertical_bias="0.962"
        app:tint="@android:color/white" />

</androidx.constraintlayout.widget.ConstraintLayout>